import {Disaster} from "../model/Disaster.ts";

export async function getDisasters(initialDate : Date, lastDate : Date, type: EnumDisasters): Disaster[]{
//TODO
    console.log(initialDate);
    console.log(lastDate);
    console.log(type);
}